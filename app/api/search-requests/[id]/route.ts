import { NextRequest, NextResponse } from "next/server";
import { getCurrentUser } from "@/lib/auth";
import { updateSearchRequestSchema } from "@/modules/search-requests/search-request.schema";
import { searchRequestService } from "@/modules/search-requests/search-request.service";
type Context = { params: Promise<{ id: string }> };
export async function GET(_: NextRequest, context: Context) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 }); const { id } = await context.params; const requestData = await searchRequestService.getSearchRequestById(user.agencyId, id); if (!requestData) return NextResponse.json({ error: "Demande introuvable" }, { status: 404 }); return NextResponse.json({ data: requestData }); }
export async function PATCH(request: NextRequest, context: Context) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 }); const { id } = await context.params; const body = await request.json(); const parsed = updateSearchRequestSchema.safeParse(body); if (!parsed.success) return NextResponse.json({ error: "Validation échouée", details: parsed.error.flatten() }, { status: 400 }); try { const updated = await searchRequestService.updateSearchRequest({ agencyId: user.agencyId, searchRequestId: id, updatedByUserId: user.id, input: parsed.data }); return NextResponse.json({ data: updated }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Erreur inconnue" }, { status: 400 }); } }
