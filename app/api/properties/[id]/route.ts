import { NextRequest, NextResponse } from "next/server";
import { getCurrentUser } from "@/lib/auth";
import { updatePropertySchema } from "@/modules/properties/property.schema";
import { propertyService } from "@/modules/properties/property.service";
type Context = { params: Promise<{ id: string }> };
export async function GET(_: NextRequest, context: Context) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 }); const { id } = await context.params; const property = await propertyService.getPropertyById(user.agencyId, id); if (!property) return NextResponse.json({ error: "Bien introuvable" }, { status: 404 }); return NextResponse.json({ data: property }); }
export async function PATCH(request: NextRequest, context: Context) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 }); const { id } = await context.params; const body = await request.json(); const parsed = updatePropertySchema.safeParse(body); if (!parsed.success) return NextResponse.json({ error: "Validation échouée", details: parsed.error.flatten() }, { status: 400 }); try { const updated = await propertyService.updateProperty({ agencyId: user.agencyId, propertyId: id, updatedByUserId: user.id, input: parsed.data }); return NextResponse.json({ data: updated }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Erreur inconnue" }, { status: 400 }); } }
